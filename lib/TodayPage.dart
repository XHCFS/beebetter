import 'package:flutter/material.dart';

class TodayPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Today Page', style: TextStyle(fontSize: 24))),
    );
  }
}