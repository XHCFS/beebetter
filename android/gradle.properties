org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Workaround for hf_xet package build issue on Linux CI
# Cargokit tries to build for all platforms including macOS
# Don't set CARGOKIT_DARWIN_PLATFORM_NAME - let Cargokit detect the platform
# If it still fails, we may need to fork hf_xet or find an alternative
